---
import { postsByYear } from "./_postsCollection";

// Get years in descending order for display
const years = Object.keys(postsByYear)
  .map((year) => parseInt(year))
  .sort((a, b) => b - a);
---

<main id="postsListWrapper" class="pt-5">
  {
    years.map((year) => {
      const postsInYear = postsByYear[year];
      if (!postsInYear || postsInYear.length === 0) return null;

      return (
        <div class="grid grid-cols-6">
          <p class="col-span-1 text-sm italic pt-4 pb-4 mr-20">{year}</p>
          {postsInYear.map((post, index) => {
            return (
              <a
                id="posts"
                href={`/posts/${post.id}`}
                class:list={[
                  "col-span-5",
                  "col-start-2",
                  "flex",
                  "items-center",
                  "justify-between",
                  "p-4",
                  "decoration-none",
                  {
                    "border-t borderBase": index > 0,
                  },
                ]}
              >
                <p>{post.data.title}</p>
                <p>
                  {post.data.pubDate.toLocaleDateString("en-US", {
                    month: "short",
                    day: "numeric",
                  })}
                </p>
              </a>
            );
          })}
        </div>
      );
    })
  }
</main>

<style>
  #posts {
    @apply md:transition-all md:ease-in-out md:duration-300;
  }
  #postsListWrapper:hover #posts {
    @apply opacity-40;
  }
  #postsListWrapper:hover #posts:hover {
    @apply opacity-100 md:duration-100;
  }
</style>
